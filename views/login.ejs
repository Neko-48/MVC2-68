<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เข้าสู่ระบบ | ระบบเยียวยา</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #0d6efd 0%, #003d99 100%);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Sarabun', sans-serif;
        }
        .login-card {
            background: white;
            padding: 2.5rem;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            width: 100%;
            max-width: 400px;
        }
        .login-header {
            text-align: center;
            margin-bottom: 2rem;
        }
        .login-header img {
            width: 80px;
            margin-bottom: 1rem;
        }
        .btn-login {
            background-color: #0d6efd;
            border: none;
            padding: 10px;
            font-weight: bold;
            transition: 0.3s;
        }
        .btn-login:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }
        .form-label {
            font-weight: 600;
            color: #495057;
        }
    </style>
</head>
<body>

<div class="login-card">
    <div class="login-header">
        <h4 class="fw-bold text-primary">ระบบเงินเยียวยา</h4>
        <p class="text-muted">กรุณาเลือกผู้ใช้งานเพื่อเข้าสู่ระบบ</p>
    </div>

    <form action="/login" method="POST">
        <div class="mb-4">
            <label for="userId" class="form-label">ชื่อผู้ใช้งาน / บทบาท</label>
            <select name="userId" id="userId" class="form-select form-select-lg" required>
                <optgroup label="เจ้าหน้าที่">
                    <option value="admin">👨‍💼 เจ้าหน้าที่บริหารระบบ (Admin)</option>
                </optgroup>
                <optgroup label="ประชาชน">
                    <% claimants.forEach(person => { %>
                        <option value="<%= person.id %>">
                            👤 <%= person.firstName %> <%= person.lastName %>
                        </option>
                    <% }) %>
                </optgroup>
            </select>
        </div>

        <button type="submit" class="btn btn-login btn-primary w-100 mb-3">
            เข้าสู่ระบบ
        </button>
        
    </form>
</div>

</body>
</html>