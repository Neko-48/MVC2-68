<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>รายการคำขอเยียวยา</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .card { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); }
        .status-pending { color: #856404; background-color: #fff3cd; padding: 4px 8px; border-radius: 20px; font-size: 0.85rem; }
        .status-approved { color: #155724; background-color: #d4edda; padding: 4px 8px; border-radius: 20px; font-size: 0.85rem; }
        .status-rejected { color: #721c24; background-color: #f8d7da; padding: 4px 8px; border-radius: 20px; font-size: 0.85rem; 
}
    </style>
</head>
<body>
<nav class="navbar navbar-dark bg-primary mb-4">
    <div class="container">
        <span class="navbar-brand">ระบบติดตามเงินเยียวยา</span>
        <a href="/logout" class="btn btn-outline-light btn-sm">ออกจากระบบ</a>
    </div>
</nav>

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3>รายการคำขอเยียวยา</h3>
        <% if (role === 'citizen') { %>
            <a href="/claims/create" class="btn btn-success">+ ยื่นคำขอใหม่</a>
        <% } %>
    </div>

    <div class="card p-3">
        <table class="table table-hover">
            <thead class="table-light">
                <tr>
                    <th>วันที่ยื่น</th>
                    <th>รหัสคำขอ</th>
                    <th>ผู้ขอเยียวยา</th>
                    <th class="text-end">จำนวนเงิน</th>
                    <th class="text-center">สถานะ</th>
                </tr>
            </thead>
            <tbody>
                <% if (claims && claims.length > 0) { %>
                    <% claims.forEach(item => { %>
                    <tr>
                        <td><%= new Date(item.date).toLocaleDateString('th-TH') %></td>
                        <td><strong><%= item.claimId %></strong></td>
                        <td><%= item.claimantName %></td>
                        <td class="text-end text-primary fw-bold"><%= item.amount.toLocaleString() %> ฿</td>
                        <td class="text-center">
                            <% if (item.status === 'approved') { %>
                                <span class="status-approved">อนุมัติแล้ว</span>
                            <% } else if (item.status === 'rejected') { %>
                                <span class="status-rejected">ไม่อนุมัติ</span>
                            <% } else { %>
                                <span class="status-pending">รอการตรวจสอบ</span>
                            <% } %>
                        </td>
                    </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="5" class="text-center py-5">
                            <p class="mb-3">ยังไม่มีคำขอสำหรับผู้ใช้นี้</p>
                            <% if (role === 'citizen') { %>
                                <a href="/claims/create" class="btn btn-success">ยื่นคำขอ</a>
                            <% } %>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>
</body>
</html>